<!-- res/layout/activity_add_expense.xml -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="16dp">

    <EditText
        android:id="@+id/expense_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        android:hint="Expense Name"
        android:inputType="text" />

    <!-- Expense Amount Input -->
    <EditText
        android:id="@+id/expense_amount"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        android:hint="Amount"
        android:inputType="numberDecimal" />

    <!-- Category Spinner (dropdown) -->
    <Spinner
        android:id="@+id/expense_category"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp" /> <!-- Increased bottom margin -->

    <!-- Date Picker -->
    <Button
        android:id="@+id/expense_date_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        android:onClick="onSelectDateClick"
        android:text="Select Date" /> <!-- Increased bottom margin -->

    <!-- Save Expense Button -->
    <Button
        android:id="@+id/save_expense_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="32dp"
        android:onClick="onSaveExpense"
        android:text="Save Expense" /> <!-- Margin to separate button from the rest -->
</LinearLayout>
